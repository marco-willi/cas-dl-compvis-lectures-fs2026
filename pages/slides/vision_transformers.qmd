---
title: "7 - Modern Vision Backbones"
params:
   images_path: "/assets/images/vision_transformers/"
---

::: {.content-hidden}
$$
{{< include /assets/_macros.tex >}}
$$
:::



## Overview {.center}

- From Sequences to Patches
- Self-Attention on Patches
- CNNs vs. ViTs
- Practical Architecture Selection


# Motivation {.center}


## Transformers in Vision

::: {.fragment}
Transformers dominate **language** — long-range dependencies, scale.
:::

::: {.fragment}
Can the **same architecture** work for images?
:::

::: {.fragment}
- CNNs: strong inductive biases (locality, translation equivariance)
- Transformers: minimal assumptions, treat input as a generic sequence
:::

::: {.fragment}
**ViT (2020, @dosovitskiy_image_2020)**: yes, with enough pretraining data.
:::


# From Sequences to Patches


## The Trick: Treat Images as Patches {.center}

::: {.fragment}
A 2-D image grid → a **sequence of patches**.
:::


## The Patch Embedding Pipeline

::: {.incremental}
1. **Divide** image into a grid of patches
2. **Flatten** each patch ($16 \times 16 \times 3 = 768$)
3. **Project** through a learnable linear layer → embedding of dim $D$
4. **Add positional embeddings** — encode spatial position
5. **Prepend [CLS] token** — image-level representation (à la BERT)
:::

::: {.fragment}
Result: a sequence of $N + 1$ embeddings → standard transformer encoder.
:::


## ViT Architecture

::: {#fig-vit-paper}
![Source: @dosovitskiy_image_2020]({{< meta params.images_path >}}vit.png){width=70%}

Image → patches → linear projection → positional embeddings → transformer encoder.
:::


## Patchifying

::: {#fig-vit-patch-embedding}
![]({{< meta params.images_path >}}patchify.png){width=60%}

Split the image into fixed-size, non-overlapping patches.
:::

::: {.fragment}
$224 \times 224$ image, $16 \times 16$ patches → **196 patches**.
:::

## Quiz: Patch Size 32 × 32

What changes if patch size goes from 16 to 32?

::: {.fragment}
- Number of patches: $\frac{224}{32}^2 = \mathbf{49}$ — **4× fewer**
:::

::: {.fragment}
- **Faster** — self-attention is quadratic in sequence length
- **Less detail** — each patch covers a larger area
- **Trade-off** — same as resolution choice in CNNs
:::


# Self-Attention on Image Patches


## Receptive Field — The Big Difference {.center}

::: {.fragment}
**CNN** — local kernel → grows gradually with depth
:::

::: {.fragment}
**ViT** — every patch attends to every other patch from **layer 1**
:::

::: {.fragment}
ViT receptive field is **global from the start**.
:::


## Strength and Weakness

::: {.incremental}
- ✅ **Long-range dependencies** for free
- ❌ **No locality prior** — must be learned from data
- ❌ Needs **far more training examples**
:::


## Quiz: Receptive Fields

A CNN with three 3×3 conv layers (stride 1) has a 7×7 receptive field.

A ViT with the same depth has a receptive field that covers...?

1. 7×7 pixels
2. The entire patch
3. The entire image

::: {.fragment}
**The entire image.**
:::

::: {.fragment}
After even **one** transformer layer, every output has aggregated information from all patches.
:::


## What Attention Learns

::: {#fig-vit-attention-maps1}

![Attention weight matrices.]({{< meta params.images_path >}}attention_matrices.png)

:::



## What Attention Learns

::: {#fig-vit-attention-maps2}

![Spatial attention maps overlaid on the image.]({{< meta params.images_path >}}attention_spatial.png)
:::


# CNNs vs. Vision Transformers


## CNNs vs. ViTs at a Glance

| Aspect | CNNs | ViTs |
|--------|------|------|
| **Input structure** | 2-D grid | patch sequence |
| **Inductive bias** | strong locality + equivariance | weaker visual priors |
| **Receptive field** | local → global | global token mixing |
| **Data efficiency** | strong with limited data | benefits strongly from pretraining |
| **Scaling** | strong, but more constrained | scales very well with data/compute |
| **Compute** | roughly linear in pixels | attention quadratic in tokens |

: {tbl-colwidths="[24,38,38]"}

## Inductive Biases — Help or Hindrance?

::: {.fragment}
CNNs build in **locality** and **translation equivariance**.
:::

::: {.fragment}
These assumptions are often useful for natural images, especially with limited data.
:::

::: {.fragment}
ViTs have weaker image-specific priors and rely more on data and pretraining.
:::

::: {.fragment}
- **Small data:** strong priors can help
- **Massive pretraining:** weaker priors can become an advantage
:::

## Quiz: When Can Locality Hurt?

Where might a strong local bias become limiting?

::: {.fragment}
- **Global layout**:  distinguishing scenes may require the arrangement of many regions, not just local patches
:::

::: {.fragment}
- **Long-range relations**:  e.g. person, leash, and dog may be far apart in the image
:::

::: {.fragment}
- **Repeated / non-local patterns**:  the relevant structure may be defined by relationships between distant regions
:::

::: {.fragment}
CNNs can capture these with enough depth and pooling, but global interactions are indirect.
:::

::: {.fragment}
ViTs model long-range interactions more directly through self-attention.
:::


# Practical Architecture Selection


## Decision Guide

::: {.incremental}
- **Limited data / limited compute:** start with a pretrained CNN or compact hybrid
- **Large-scale pretraining available:** ViT-style models are strong defaults
- **Need early global context:** ViT or hybrid architecture
- **Edge deployment / fast inference:** efficient CNN or efficient hybrid
- **In practice:** pretraining data, training recipe, and adaptation strategy often matter as much as the backbone
:::

## Hybrid Architectures {.center}

::: {.fragment}
**Convs early** (efficient local features) + **Transformers late** (global reasoning).
:::

::: {.fragment}
Examples: **FastViT** (@vasu_fastvit_2023), **EfficientViT**.
:::

::: {.fragment}
Conversely: **ConvNeXt** modernizes ResNet with ViT-style training and design.
:::




## Beyond Classification — Multimodal ViTs

::: {.incremental}
- **BLIP-2** (@li_blip-2_2023) — frozen ViT + LLM via Q-Former → visual QA
- **Flamingo** (@alayrac_flamingo_2022) — few-shot multimodal across image-text sequences
:::

::: {.fragment}
The patch-embedding approach has become a **universal interface** between visual and language representations.
:::


# Summary {.center}


## Key Takeaways

::: {.incremental}
- ViTs treat images as **sequences of patches** — works with the standard transformer encoder
- **Global receptive field** from layer 1 — strength *and* weakness
- CNNs: strong priors, data-efficient · ViTs: flexible, scale-hungry
- **Hybrids** (FastViT, EfficientViT) combine the strengths
- Pretraining + adaptation strategy often matter more than the backbone
:::


# References

::: {style="font-size: 50%;"}

::: {#refs}
:::

:::
